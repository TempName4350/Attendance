# build script that will run frontend and backend. frontend runs according to static files in target/classes/static, generated when the build script is run from whatever is in the angular client folder. backend runs on spring boot. to view UI, visit localhost:8080
mvn spring-boot:run
